export enum Scope {
  // openid standard scopes
  OPENID = "openid",
  ADDRESS = "address",
  EMAIL = "email",
  PHONE = "phone",
  PROFILE = "profile",

  // identity service scopes
  PRIVATE = "private",
}
